import React from "react";
import "../styles/notask.css";

export default function NoTask() {
  return (
    <div className="no-task">
      You have not created any task yet. To create a task simply click on the
      add task button in the right navigation bar and we simply keep track of
      everything.
    </div>
  );
}
