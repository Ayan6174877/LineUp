import React from "react";

function NotFound() {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "60vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>We're sorry, but we think that you are just at the wrong place</h1>
    </div>
  );
}

export default NotFound;
